<?xml version="1.0" encoding="utf-8"?>
<!--
  Shared surface for search boxes, list rows and panels.

  Five screens draw with this, so giving it the launcher's card look — rounded,
  filled with the surface colour, edged with the soft border — carries the theme
  into all of them at once instead of one layout at a time.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="@dimen/_10sdp" />
    <padding
        android:bottom="@dimen/padding_small"
        android:left="@dimen/padding_small"
        android:right="@dimen/padding_small"
        android:top="@dimen/padding_small" />
    <solid android:color="@color/surface" />
    <stroke android:width="1dp" android:color="@color/border_soft" />
</shape>
