<?xml version="1.0" encoding="utf-8"?>
<!-- Marks the chosen swatch in the icon editor. A tinted plate rather than a
     border: the icons themselves are rounded, and a rectangular outline around
     them read as a second, misaligned frame. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/accent_ripple" />
    <corners android:radius="12dp" />
    <stroke
        android:width="1dp"
        android:color="@color/accent" />
</shape>
